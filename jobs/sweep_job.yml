$schema: https://azuremlschemas.azureedge.net/latest/sweepJob.schema.json
type: sweep

display_name: amazon_review_sweep
experiment_name: amazon_review_sweep_exp

trial:
  code: ../src
  command: >
    python train.py
    --train_data ${{inputs.train_data}}
    --val_data ${{inputs.val_data}}
    --test_data ${{inputs.test_data}}
    --output ${{outputs.model_output}}
    --max_iter ${{search_space.max_iter}}
    --C ${{search_space.C}}

  environment:
    image: mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu20.04
    conda_file: ../env/conda.yml

inputs:
  train_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_train@latest

  val_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_val@latest

  test_data:
    type: uri_folder
    path: azureml:amazon_review_merged_features_test@latest

outputs:
  model_output:
    type: uri_folder

compute: azureml:lab4-cluster-60304739

sampling_algorithm: random

search_space:
  C:
    type: uniform
    min_value: 0.001
    max_value: 1.0

  max_iter:
    type: choice
    values: [100, 200, 300]

objective:
  primary_metric: val_accuracy
  goal: maximize

limits:
  max_total_trials: 6
  max_concurrent_trials: 2